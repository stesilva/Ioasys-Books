import React from   'react';
import { Form } from '../../components/Form';
import { StyledLoginWrapper } from './styled';

export const Login = () => {
    return(
        <StyledLoginWrapper>
            <Form></Form>
        </StyledLoginWrapper>
    );
}